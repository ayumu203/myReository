#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin >> n;
    vector<int> q(n,0);
    vector<int> a(n,0);
    vector<int> b(n,0);
    for(int i=0; i<n; i++)cin >> q[i];
    for(int i=0; i<n; i++)cin >> a[i];
    for(int i=0; i<n; i++)cin >> b[i];
    return 0;
}